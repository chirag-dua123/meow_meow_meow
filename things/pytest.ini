# pytest.ini
[pytest]
# Directory where tests are located
testpaths = tests

# File pattern to discover tests
python_files = test_*.py

# Optional: markers for categorizing tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    unit: marks unit tests
